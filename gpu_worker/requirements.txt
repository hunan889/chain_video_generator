# GPU Worker service dependencies
redis[asyncio]>=5.0
aiohttp>=3.9
python-dotenv
cos-python-sdk-v5
fakeredis[aioredis]>=2.20   # test only
pytest>=8.0                  # test only
pytest-asyncio>=0.23         # test only
pytest-cov                   # test only
